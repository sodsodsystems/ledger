// System Configuration
// Replace with your GitHub token and the repository where data.json will be stored.
window.LEDGER_CONFIG = {
  GITHUB_TOKEN: "YOUR_GITHUB_TOKEN",
  GITHUB_REPO: "your-username/your-private-repo"
};
